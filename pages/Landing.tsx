import React from 'react';
import MarketingHome from './Marketing/Home';

/**
 * Landing page at / – public entry point.
 * App (dashboard, coach, etc.) lives under /app.
 */
const Landing: React.FC = () => <MarketingHome />;

export default Landing;
